[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "always"

[environment]
NODE_ENV = "production"

[nixpacks]
# Explicitly set to Node.js provider
provider = "node"